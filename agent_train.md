一、技术路径

1. 总体架构

- 建议将本阶段定义为“旅游内容标准化生产 Agent”，职责仅包含：多模态解析、内容切割、四类归档、景点标签抽取、一句话总结生成、标准化入库。
- 整体采用 采集接入层 -> 媒体预处理层 -> 多模态理解层 -> 内容标准化层 -> 质检评估层 -> 标准库入库层 的流水线架构，便于后续与下游“清洗/筛选 Agent”解耦。
- 处理对象统一抽象为 内容任务 Job ，每个 Job 仅保存内部任务 ID，不保留来源平台、作者、链接、账号等原始标识信息。
- 处理结果只输出标准化内容数据库，核心字段严格限制为： 分类标签 、 内容片段 、 一句话总结 、 关联景点标签 。
- 建议通过异步任务编排实现多模态并行处理，避免视频转写、镜头切分、图像识别等高耗时步骤阻塞主流程。
2. 全流程数据流转

- 输入接入 ：接收三类输入，分别为图文、带音频视频/音视频、纯视频。
- 格式识别 ：根据 MIME、编码信息、是否含音轨自动分流到不同处理链路。
- 媒体预处理 ：
  - 带音频视频/音视频：抽取音轨，标准化为统一采样率单声道音频。
  - 纯视频：抽帧并进行镜头切换检测。
  - 图文：图片与正文分离，建立图文对应关系。
- 内容理解 ：
  - 音视频链路：ASR 转写后，结合原视频关键帧或时间片段，由 LLM 执行片段切割、四类分类、景点标签抽取、一句话总结。
  - 纯视频链路：多模态模型读取视频流/关键帧序列，按镜头切换节点切段，识别每段视觉实体与场景语义，映射到四大板块并生成总结。
  - 图文链路：图像识别模型提取景点/餐饮/交通/住宿实体，结合正文做跨模态对齐，再由 LLM 完成片段化与总结。
- 标准化处理 ：把所有模态输出统一转成同一内容对象结构，消除不同来源格式差异。
- 规则校验 ：校验四类标签合法性、片段粒度是否合规、总结是否为单句、景点标签是否存在。
- 入库 ：仅写入标准化字段，不写原文、原视频地址、原图片 URL、原作者信息、平台信息。
- 交付下游 ：对下游只提供标准化内容表/接口，不在本阶段做去重、清洗、筛选、排序。
3. 推荐实现方式

- 建议采用“规则 + 模型”混合架构，而不是纯大模型自由生成。
- 规则负责：媒体分流、镜头边界、字段校验、敏感信息剥离、输出结构约束。
- 模型负责：语义理解、跨模态融合、四类归类判断、景点标签抽取、单句总结生成。
- 所有模型输出均要求 JSON Schema 约束，避免自由文本导致入库失败。
- 对每个片段保留 处理置信度 作为系统内部字段可选存在于中间层，但最终交付数据库若需严格遵守“四字段”，则不进入最终表，只用于过程质检。
- 若后续规模较大，建议拆分为微服务： media-service 、 asr-service 、 vision-service 、 llm-orchestrator 、 normalizer-service 、 storage-service 。
2、核心技术选型及适配方案

1. 编排与服务框架

- 推荐后端主框架： Python + FastAPI 。
- 原因：Python 在音视频处理、ASR、CV、多模态推理、任务编排方面生态最成熟。
- 异步任务队列推荐： Celery + Redis 或 RabbitMQ ，用于长时视频处理与批量任务调度。
- 若需要工作流可视化编排，可选 Temporal 或 Prefect ；若第一期追求快速落地， FastAPI + Celery 足够。
2. 带音频视频/音视频处理链路

- 音轨提取： FFmpeg 。
- ASR 模型推荐优先级：
  - 首选： Whisper large-v3 或商业高精度 ASR API。
  - 中文场景可增强： FunASR 。
- 适配方案：
  - 先用 FFmpeg 抽取音频。
  - 做 VAD 分段，减少长音频识别误差。
  - ASR 输出时间戳文本。
  - 再将转写文本与关键帧时间对齐，交由 LLM 切片、分类、总结。
- 优势：满足“先转写，再由大模型完成内容切割、分类与归纳”的硬性要求。
3. 纯视频处理链路

- 镜头切换检测工具：
  - 开源优先： PySceneDetect 。
  - 若需要更强鲁棒性，可叠加 TransNetV2 。
- 视觉理解模型：
  - 云端优先： GPT-4o 、 Gemini 1.5/2.x Pro 、 Qwen2.5-VL 商业/API版。
  - 本地可选： Qwen2.5-VL 、 InternVL 、 MiniCPM-V 。
- 适配方案：
  - 先按镜头边界切段。
  - 每段抽取关键帧或短视频 clip。
  - 多模态模型识别该镜头中出现的景点实体、交通设施、餐食、住宿场景。
  - 再映射到四大分类并生成一句话摘要。
- 注意：纯视频链路需重点解决“视觉实体识别”与“镜头语义缺省”问题，必要时可引入 OCR 从画面字幕提取辅助信息。
4. 图文处理链路

- OCR 推荐： PaddleOCR 或商业 OCR API。
- 图像识别模型推荐：
  - 通用视觉识别： Qwen2.5-VL 、 GPT-4o 、 Gemini 。
  - 若强调地标/景点识别，可增加图像检索或地标识别模型作为补充。
- 适配方案：
  - 逐图识别图片中的景点实体、交通工具、菜品、酒店/房型等对象。
  - 同步抽取正文中的对应描述。
  - 建立“图片-正文段落”对齐关系。
  - 由 LLM 基于图像识别结果 + 文本上下文进行统一切割、分类与单句总结。
- 对图文内容不建议只依赖 OCR，必须保留图像语义识别能力，否则景点实体召回不足。
5. 大语言模型选型

- 主模型职责：内容切割、四类分类、景点标签抽取、一句话总结、结构化输出。
- 推荐选型：
  - 云端优先： GPT-4.1/4o 、 Gemini 2.x Pro 、 Claude 3.5/3.7 、 Qwen-Max 。
  - 私有化可选： Qwen2.5-72B-Instruct 、 DeepSeek 系列。
- 适配要求：
  - 支持长上下文，适合长攻略和长视频转写。
  - 支持 JSON Schema 或 function calling，便于严格结构化。
  - 最好支持中英混输、旅行实体识别、多轮纠偏。
6. 存储层选型

- 主数据库推荐： PostgreSQL 。
- 原因：结构化字段清晰、事务稳定、支持 JSONB 扩展、后期可平滑接入向量扩展。
- 如果后续下游需要语义召回，可预留 pgvector ，但本阶段不强依赖。
- 对象存储仅作为临时处理中转可选存在，不作为最终交付库的一部分。
3、内容切割、分类、归纳的标准化逻辑定义

1. 标准化目标

- 任一输入内容都要被切成最小可复用语义片段。
- 每个片段只能归属四大核心板块之一： 景点 、 饮食 、 交通 、 住宿 。
- 每个片段必须生成一条精准的一句话总结。
- 每个片段必须带 关联景点标签 ，若无法识别具体景点，则使用标准占位值，如“未知景点/未指明区域”，但不得为空。
2. 切割原则

- 以“单一主题完整表达”为最小切割单位，而不是机械按句号或固定时长切。
- 文本切割依据：
  - 主题转换。
  - 场景转换。
  - 类别转换。
  - 景点对象转换。
- 视频切割依据：
  - 优先按镜头切换点切割。
  - 若同一镜头内语义明显变化，可二次细分。
  - 带音轨视频可融合 ASR 时间片与镜头边界进行对齐切片。
- 图文切割依据：
  - 每张图单独识别。
  - 若多图表达同一地点/主题，可聚合为一个片段。
  - 正文中与图片强关联的说明应合并进入同一片段。
3. 四类分类定义

- 景点 ：景区、地标、自然风光、博物馆、街区、城市打卡点、游玩体验项目。
- 饮食 ：餐厅、小吃、特色菜、饮品、用餐建议、排队信息、口味评价。
- 交通 ：航班、火车、地铁、公交、打车、自驾、换乘、步行路线、到达方式。
- 住宿 ：酒店、民宿、青旅、房型、地理位置、入住体验、价格带、设施服务。
- 冲突裁决规则：
  - 以该片段“主要服务目的”为准。
  - 例如“景点附近推荐酒店”若重点在酒店，则归 住宿 。
  - “去某景点怎么坐车”归 交通 。
  - “某景区内必吃小吃”归 饮食 。
4. 一句话总结生成规则

- 长度建议控制在 20-45 个中文字符。
- 必须是完整单句，不使用分号、枚举、换行。
- 必须包含核心对象 + 关键信息点。
- 总结模板建议：
  - 景点 ： [景点名]适合[游玩特征]，亮点是[核心卖点]。
  - 饮食 ： [店名/菜品]以[特色]见长，适合[场景/人群]。
  - 交通 ： 前往[景点/区域]建议采用[方式]，优势是[效率/成本]。
  - 住宿 ： [酒店/区域住宿]适合[人群]，优势在于[位置/设施/性价比]。
- 生成后需做规则校验：
  - 是否为一句。
  - 是否含核心实体。
  - 是否出现主观空泛词堆砌。
  - 是否与分类一致。
5. 关联景点标签规则

- 若片段本身直接描述景点，景点标签为该景点名。
- 若片段为饮食/交通/住宿，但明显服务于某景点或景区，景点标签绑定该景点。
- 若只指向城市区域，可使用“城市-片区”作为景点标签标准名。
- 多景点关联时，建议当前阶段保留单主标签，降低库结构复杂度；若业务强依赖多标签，可通过中间表扩展，但最终对下游暴露时仍可只出主标签。
4、数据库 Schema 设计方案

1. 最终交付表

- 表名建议： travel_content_blocks
- 字段建议：
  - id ：主键，UUID
  - category ：分类标签，枚举值，仅允许 景点/饮食/交通/住宿
  - content_block ：内容片段，文本
  - summary_one_line ：一句话总结，文本
  - poi_tag ：关联景点标签，文本
  - created_at ：创建时间
  - updated_at ：更新时间
- 若严格要求“仅保留四类核心字段”，则业务交付视图只暴露：
  - category
  - content_block
  - summary_one_line
  - poi_tag
- id/created_at/updated_at 仅作为系统内部字段，不对下游业务数据层开放也可。
2. 约束设计

- category 非空，且必须属于四枚举值。
- content_block 非空，长度设置下限，防止无意义片段。
- summary_one_line 非空，长度限制例如 20-80 字符。
- poi_tag 非空，无法识别时写标准兜底值。
- 不允许存储字段：
  - 原始 URL
  - 平台名
  - 用户名/作者 ID
  - 原始媒体路径
  - 原始标题
  - 原始发布时间
  - 原始账号标识
- 通过入库前脱敏校验器扫描禁止字段内容。
3. 推荐的中间层表

- 本阶段虽最终只交付四字段，但实现时建议保留中间表，便于排错：
  - ingest_jobs
  - media_segments
  - asr_transcripts
  - multimodal_parse_results
  - normalization_audit_logs
- 中间层表仅内部使用，不向下游开放；项目若对隐私要求极高，可设置自动过期清理。
5、功能验证与效果评估标准

1. 功能正确性验证

- 输入分流正确率：图文、纯视频、音视频识别准确。
- 音轨提取成功率：带音轨视频应稳定抽出音频。
- ASR 成功率：中文旅游语料下转写可用率达标。
- 镜头切分有效率：纯视频镜头边界漏检、误检可控。
- 结构化输出成功率：LLM 输出符合 JSON Schema 的比例需高。
- 入库成功率：标准化结果写库不中断。
2. 内容质量评估

- 切割合理性：
  - 是否做到一段一主题。
  - 是否避免过碎或过粗。
- 分类准确率：
  - 四类标签是否符合业务定义。
- 景点标签准确率：
  - 是否能正确绑定到对应景点/区域。
- 总结质量：
  - 是否为一句话。
  - 是否信息完整。
  - 是否避免空泛套话。
- 推荐评估指标：
  - 分类准确率
  - 景点标签准确率
  - 切割边界 F1
  - 单句总结人工满意度
  - 结构化输出通过率
3. 评测方法

- 建立一套人工标注基准集，覆盖三类输入模态。
- 每类至少标注：
  - 正常样本
  - 混合样本
  - 噪声样本
  - 极端样本
- 采用“双评审 + 冲突仲裁”形成 gold data。
- 每次模型或规则更新后执行固定回归测试，防止分类漂移与总结风格漂移。
4. 上线门槛建议

- 分类准确率不低于 90%
- 景点标签准确率不低于 85%
- 单句总结可用率不低于 90%
- 结构化输出通过率不低于 95%
- 带音频视频整体成功处理率不低于 95%
二、准备清单

1. AI 模型资源清单

- 大语言模型
  - 用途：切割、分类、景点标签抽取、一句话总结、结构化输出。
  - 推荐： GPT-4o/4.1 、 Gemini 2.x Pro 、 Qwen-Max 、 Claude 3.7 。
  - 部署方式：云 API 优先；若涉敏感数据可走私有化推理。
  - 权限要求：稳定 API Key、并发额度、长上下文额度、结构化输出支持。
- 多模态模型
  - 用途：纯视频理解、图文联合理解、关键帧识别、画面内容分类。
  - 推荐： GPT-4o 、 Gemini 、 Qwen2.5-VL 、 InternVL 。
  - 部署方式：云端优先，本地 GPU 私有化作为成本/隐私备选。
  - 权限要求：视频/图片输入能力、较大请求体支持。
- ASR 模型
  - 用途：音频转写。
  - 推荐： Whisper large-v3 、 FunASR 、商业 ASR。
  - 部署方式：本地 GPU 或云 API。
  - 权限要求：长音频支持、时间戳输出、中文识别效果。
- 图像识别/OCR 模型
  - 用途：景点实体识别、画面文字抽取、菜单/路牌/酒店名识别。
  - 推荐： PaddleOCR + 多模态视觉模型组合。
  - 部署方式：OCR 可本地，视觉识别可 API 或本地部署。
  - 权限要求：批量调用、图片尺寸上限、中文场景鲁棒性。
2. 音视频处理工具链与依赖

- FFmpeg ：音轨提取、转码、抽帧、时长裁剪。
- PySceneDetect ：镜头边界检测。
- OpenCV ：帧处理、图像采样、基础视觉预处理。
- MoviePy 或 PyAV ：Python 侧视频读写辅助。
- librosa / torchaudio ：音频分析与预处理。
- webrtcvad ：语音活动检测，可提升 ASR 分段质量。
- 运行环境要求：
  - Windows 开发可行，但正式部署更建议 Linux。
  - 若做本地模型推理，建议配备 NVIDIA GPU，显存优先 24GB+ 。
  - 需安装 CUDA 、 cuDNN 、匹配版本的 PyTorch 。
3. 数据库选型与部署配置

- 首选： PostgreSQL 15+
- 最低配置建议：
  - CPU：4 核以上
  - 内存：8GB 以上
  - SSD：100GB 起
- 配置要求：
  - UTF-8 编码
  - 开启定期备份
  - 建立分类字段索引、景点标签索引
  - 若后续需要语义检索，可预装 pgvector
- 安全要求：
  - 数据库账号分权
  - 生产环境内网访问
  - 日志脱敏
  - 不允许在 DB 中持久化来源身份标识
4. 开发技术栈与环境配置

- 后端： Python 3.11+
- Web/API： FastAPI
- 任务队列： Celery + Redis
- 数据库 ORM： SQLAlchemy 或 SQLModel
- 数据校验： Pydantic
- 模型编排：自研 Orchestrator 或 LangChain/LlamaIndex 轻量使用
- 部署容器： Docker 、 Docker Compose
- 配置管理： .env + 密钥管理服务
- 日志与监控：
  - Prometheus + Grafana
  - ELK 或云日志服务
- 开发环境要求：
  - Git 仓库与分支规范
  - API 密钥管理机制
  - 测试环境数据库
  - 可复现的依赖锁定，如 poetry 或 pip-tools
5. 功能测试样例素材要求

- 图文样例
  - 至少 50 份旅游攻略图文。
  - 覆盖单景点、多景点、跨城、多餐饮、多住宿场景。
  - 需包含图片内文字、正文描述、省略表达、口语表达。
- 带音频视频样例
  - 至少 30 条。
  - 覆盖普通话、方言、环境噪声、背景音乐、多人说话。
  - 时长覆盖 30 秒 - 10 分钟 。
- 纯视频样例
  - 至少 20 条。
  - 覆盖稳定镜头、频繁转场、字幕视频、无字幕视频、航拍、室内餐厅、酒店探房、交通路线拍摄。
- 标注要求
  - 每份样例需有人工标注的切割结果、分类标签、景点标签、标准一句话总结。
  - 标注中需专门纳入难例：同段混合多主题、无明确景点名、模糊住宿推荐、景区内餐饮与景点混杂等。
三、落地建议

- 第一阶段优先做 MVP ：先打通 带音频视频 -> ASR -> LLM 标准化 -> 入库 ，因为该链路最符合需求主干。
- 第二阶段补齐 纯视频镜头识别 与 图文跨模态对齐 ，再统一归一化。
- 第三阶段做评测闭环与规则优化，把“切割边界、分类冲突、景点标签召回”三类问题持续迭代。
- 若目标是尽快上线，建议采用“云模型 API + 本地 FFmpeg/PostgreSQL + Python 编排”的组合，开发速度最快、风险最低。
- 若目标是私有化交付，需提前锁定 GPU、模型许可、推理框架和视频处理吞吐方案，否则项目周期会明显拉长。
如果你需要，我可以下一步直接把这份内容整理成一版可放进 PRD/技术方案文档的正式章节结构。