一、技术路径

1. 总体架构

- 建议将本阶段定义为“旅游内容标准化生产 Agent”，职责仅包含：调用 Kimi API 完成 OCR 与语义理解、调用讯飞 skill 完成 ASR 解析、内容切割、四类归档、名称抽取、详细地点补全、一句话描述生成、标准化入库。
- 整体采用 采集接入层 -> 媒体预处理层 -> 文本提取层 -> 语义理解层 -> 内容标准化层 -> 质检评估层 -> 标准库入库层 的流水线架构，便于后续与下游“清洗/筛选 Agent”解耦。
- 处理对象统一抽象为 内容任务 Job ，每个 Job 仅保存内部任务 ID，不保留来源平台、作者、链接、账号等原始标识信息。
- 处理结果只输出标准化内容数据库，核心字段严格限制为： 类别 、 名称 、 详细地点 、 一句话描述 。
- 建议通过异步任务编排实现 Kimi OCR、讯飞 ASR skill 与文本标准化任务并行处理，避免长音频转写和批量图片识别阻塞主流程。
2. 全流程数据流转

- 输入接入 ：接收两类输入，分别为图文、带音频视频/音视频。
- 格式识别 ：根据 MIME、编码信息、是否含音轨自动分流到不同处理链路。
- 媒体预处理 ：
  - 带音频视频/音视频：抽取音轨，标准化为统一采样率单声道音频。
  - 图文：图片与正文分离，建立图文对应关系。
- 内容理解 ：
  - 音视频链路：调用讯飞 `ifly-speed-transcription` skill 转写后，直接以转写文本为主输入，由 Kimi API 执行片段切割、四类分类、名称抽取、详细地点补全、一句话描述生成。
  - 图文链路：调用 Kimi API 读取图片或 PDF 中的文字与版面信息，再结合正文做文本合并与对齐，由 Kimi API 完成片段化、名称抽取、地点抽取与描述生成。
- 标准化处理 ：把所有模态输出统一转成同一内容对象结构，消除不同来源格式差异。
- 规则校验 ：校验四类标签合法性、名称是否明确、详细地点是否存在、描述是否为单句。
- 入库 ：仅写入标准化字段，不写原文、原视频地址、原图片 URL、原作者信息、平台信息。
- 交付下游 ：对下游只提供标准化内容表/接口，不在本阶段做去重、清洗、筛选、排序。
3. 推荐实现方式

- 建议采用“规则 + 模型”混合架构，而不是纯大模型自由生成。
- 规则负责：媒体分流、字段校验、敏感信息剥离、输出结构约束。
- 模型负责：文本语义理解、四类归类判断、名称抽取、地点补全、单句描述生成。
- 所有模型输出均要求 JSON Schema 约束，避免自由文本导致入库失败。
- 对每个片段保留 处理置信度 作为系统内部字段可选存在于中间层，但最终交付数据库若需严格遵守“四字段”，则不进入最终表，只用于过程质检。
- 若后续规模较大，建议拆分为微服务： media-service 、 ifly-asr-gateway 、 kimi-gateway 、 normalizer-service 、 storage-service 。
2、核心技术选型及适配方案

1. 编排与服务框架

- 推荐后端主框架： Python + FastAPI 。
- 原因：Python 便于封装 Kimi API 与讯飞 skill 调用、处理音视频文件、做文本清洗和任务编排。
- 异步任务队列推荐： Celery + Redis 或 RabbitMQ ，用于长时视频处理与批量任务调度。
- 若需要工作流可视化编排，可选 Temporal 或 Prefect ；若第一期追求快速落地， FastAPI + Celery 足够。
2. 带音频视频/音视频处理链路

- 音轨提取： FFmpeg 。
- ASR 能力统一采用：讯飞 `ifly-speed-transcription` skill 。
- 适配方案：
  - 先用 FFmpeg 抽取音频。
  - 将音频标准化为讯飞 skill 可接受格式后，调用 `d:\Dev\projects\dy-hackathon\iFly-Skills\ifly-speed-transcription\scripts\transcribe.py`。
  - ASR 输出时间戳文本与分段结果。
  - 再将转写文本直接交由 Kimi API 执行分类、名称抽取、地点补全与一句话描述生成。
- 优势：满足“先转写，再由大模型完成内容切割、分类与归纳”的硬性要求，同时保留讯飞在音频转写上的成熟能力。
3. 图文处理链路

- OCR 能力统一采用： Kimi API 。
- 适配方案：
  - 图片或 PDF 调用 `d:\Dev\projects\dy-hackathon\API_keys.md` 中配置的 Kimi API。
  - 逐图或逐文档执行 OCR，提取招牌、菜单、路牌、酒店名、景点文字说明等文本信息。
  - 同步抽取正文中的对应描述。
  - 建立“图片-正文段落”对齐关系。
  - 由 Kimi API 基于 OCR 结果 + 文本上下文进行统一切割、分类、名称抽取、地点补全与一句话描述生成。
4. 大语言模型选型

- 主模型职责：OCR、内容切割、四类分类、名称抽取、详细地点补全、一句话描述生成、结构化输出。
- 当前选型： Kimi API
- 对应配置：
  - Base URL： `https://api.kimi.com/coding/v1`
  - Model： `kimi-for-coding`
- 适配要求：
  - 支持长上下文，适合长攻略和长视频转写。
  - 支持 JSON Schema 或 function calling，便于严格结构化。
  - 最好支持中英混输、旅行实体识别、多轮纠偏。
5. 存储层选型

- 主数据库推荐： MySQL 8.0+ 。
- 原因：当前仅需承载类别、名称、详细地点、一句话描述四类结构化字段，MySQL 足以满足 MVP 阶段的入库、查询与筛选需求。
- 对象存储仅作为临时处理中转可选存在，不作为最终交付库的一部分。
3、内容切割、分类、归纳的标准化逻辑定义

1. 标准化目标

- 任一输入内容都要被切成最小可复用语义片段。
- 每个片段只能归属四大核心板块之一： 景点 、 饮食 、 交通 、 住宿 。
- 每个片段必须抽取出一个明确的名称。
- 每个片段必须生成一条精准的一句话描述。
- 每个片段必须带详细地点；若原攻略未明确提供，可先由 Kimi API 基于上下文补全，仍无法确认时再联网检索并回填，最终仍无法确认则写入“未明确地点”。
2. 切割原则

- 以“单一主题完整表达”为最小切割单位，而不是机械按句号或固定时长切。
- 文本切割依据：
  - 主题转换。
  - 场景转换。
  - 类别转换。
  - 景点对象转换。
- 音视频切割依据：
  - 以 ASR 转写后的主题转换为主。
  - 若同一段转写中出现明显类别变化，可二次细分。
  - 以时间戳文本片段作为切割参考，而不是按镜头边界切割。
- 图文切割依据：
  - 每张图单独识别。
  - 若多图表达同一地点/主题，可聚合为一个片段。
  - 正文中与图片强关联的说明应合并进入同一片段。
3. 四类分类定义

- 景点 ：景区、地标、自然风光、博物馆、街区、城市打卡点、游玩体验项目。
- 饮食 ：餐厅、小吃、特色菜、饮品、用餐建议、排队信息、口味评价。
- 交通 ：航班、火车、地铁、公交、打车、自驾、换乘、步行路线、到达方式。
- 住宿 ：酒店、民宿、青旅、房型、地理位置、入住体验、价格带、设施服务。
- 冲突裁决规则：
  - 以该片段“主要服务目的”为准。
  - 例如“景点附近推荐酒店”若重点在酒店，则归 住宿 。
  - “去某景点怎么坐车”归 交通 。
  - “某景区内必吃小吃”归 饮食 。
4. 一句话描述生成规则

- 长度建议控制在 20-45 个中文字符。
- 必须是完整单句，不使用分号、枚举、换行。
- 必须包含核心对象 + 关键信息点。
- 描述模板建议：
  - 景点 ： [景点名]适合[游玩特征]，亮点是[核心卖点]。
  - 饮食 ： [店名/菜品]以[特色]见长，适合[场景/人群]。
  - 交通 ： 前往[景点/区域]建议采用[方式]，优势是[效率/成本]。
  - 住宿 ： [酒店/区域住宿]适合[人群]，优势在于[位置/设施/性价比]。
- 生成后需做规则校验：
  - 是否为一句。
  - 是否含核心实体。
  - 是否出现主观空泛词堆砌。
  - 是否与分类一致。
5. 详细地点补全规则

- 若攻略正文已给出明确地址、商圈、景区或城市区域，直接作为详细地点写入。
- 若正文仅给出模糊位置，如“外滩附近”“市中心那家店”，先由 Kimi API 结合上下文补全为“城市-区域-地点”格式。
- 若正文完全缺失地点，但名称明确，可通过联网搜索公开信息后由 Kimi API 归一化为详细地点。
- 若仍无法确认具体地址，则写入标准兜底值“未明确地点”。
4、数据库 Schema 设计方案

1. 最终交付表

- 表名建议： travel_structured_items
- 字段建议：
  - id ：主键，BIGINT AUTO_INCREMENT
  - category ：分类标签，枚举值，仅允许 景点/饮食/交通/住宿
  - name ：名称，文本
  - location ：详细地点，文本
  - summary ：一句话描述，文本
  - created_at ：创建时间
  - updated_at ：更新时间
- 最简单可用的形式：一张结果表直接存四个业务字段，系统内部仅额外保留 `id/created_at/updated_at` 便于维护。
- 若严格要求“仅保留四类核心字段”，则业务交付视图只暴露：
  - category
  - name
  - location
  - summary
- id/created_at/updated_at 仅作为系统内部字段，不对下游业务数据层开放也可。
- MySQL 建议使用 `utf8mb4` 字符集与 `utf8mb4_unicode_ci` 排序规则，确保中文与混合文本兼容。
2. 约束设计

- category 非空，且必须属于四枚举值。
- name 非空，长度设置下限，防止无意义对象名。
- location 非空，无法识别时写标准兜底值“未明确地点”。
- summary 非空，长度限制例如 20-80 字符。
- 不允许存储字段：
  - 原始 URL
  - 平台名
  - 用户名/作者 ID
  - 原始媒体路径
  - 原始标题
  - 原始发布时间
  - 原始账号标识
- 通过入库前脱敏校验器扫描禁止字段内容。
3. 推荐的中间层表

- 本阶段虽最终只交付四字段，但实现时建议保留中间表，便于排错：
  - ingest_jobs
  - media_segments
  - asr_transcripts
  - structured_parse_results
  - normalization_audit_logs
- 中间层表仅内部使用，不向下游开放；项目若对隐私要求极高，可设置自动过期清理。
5、功能验证与效果评估标准

1. 功能正确性验证

- 输入分流正确率：图文、音视频识别准确。
- 音轨提取成功率：带音轨视频应稳定抽出音频。
- 讯飞转写 Skill 成功率：中文旅游语料下转写可用率达标。
- 结构化输出成功率：Kimi API 输出符合 JSON Schema 的比例需高。
- 入库成功率：标准化结果写库不中断。
2. 内容质量评估

- 切割合理性：
  - 是否做到一段一主题。
  - 是否避免过碎或过粗。
- 分类准确率：
  - 四类标签是否符合业务定义。
- 详细地点准确率：
  - 是否能正确补全或绑定到对应地址、区域或城市片区。
- 描述质量：
  - 是否为一句话描述。
  - 是否信息完整。
  - 是否避免空泛套话。
- 推荐评估指标：
  - 分类准确率
  - 详细地点准确率
  - 切割边界 F1
  - 一句话描述人工满意度
  - 结构化输出通过率
3. 评测方法

- 建立一套人工标注基准集，覆盖两类输入模态。
- 每类至少标注：
  - 正常样本
  - 混合样本
  - 噪声样本
  - 极端样本
- 采用“双评审 + 冲突仲裁”形成 gold data。
- 每次模型或规则更新后执行固定回归测试，防止分类漂移与总结风格漂移。
4. 上线门槛建议

- 分类准确率不低于 90%
- 详细地点准确率不低于 85%
- 一句话描述可用率不低于 90%
- 结构化输出通过率不低于 95%
- 带音频视频整体成功处理率不低于 95%
二、准备清单

1. AI 模型资源清单

- 大语言模型
  - 用途：OCR、切割、分类、名称抽取、详细地点补全、一句话描述生成、结构化输出。
  - 当前采用： Kimi API
  - 对应配置： `https://api.kimi.com/coding/v1` + `kimi-for-coding`
  - 权限要求：Kimi API Key、并发额度、长上下文额度、结构化输出支持。
- 讯飞极速转写 Skill
  - 对应目录： `d:\Dev\projects\dy-hackathon\iFly-Skills\ifly-speed-transcription`
  - 用途：音频转写。
  - 调用方式：复用仓库中的 `scripts/transcribe.py`。
  - 权限要求： `XFEI_APP_ID` 、 `XFEI_API_KEY` 、 `XFEI_API_SECRET`，以及极速转写服务授权。
- Kimi OCR 能力
  - 对应配置来源： `d:\Dev\projects\dy-hackathon\API_keys.md`
  - 用途：图片 OCR、PDF OCR、版面理解与文本抽取。
  - 调用方式：通过 Kimi API 直接处理图片/PDF 输入。
  - 权限要求：Kimi API Key 与图片/PDF 输入能力可用。
2. 音视频处理工具链与依赖

- FFmpeg ：音轨提取、转码、抽帧、时长裁剪。
- OpenCV ：帧处理、图像采样、基础视觉预处理。
- iFly-Skills ：承载 ASR 的 skill 脚本与鉴权实现。
- requests ：讯飞 skill 脚本依赖，用于访问讯飞开放平台 API。
- 运行环境要求：
  - Windows 开发可行，但正式部署更建议 Linux。
  - 由于 OCR 与 LLM 改为调用 Kimi API、ASR 调用讯飞云端 skill，本地只需 CPU 跑 FFmpeg、业务服务和 skill 调用脚本即可。
  - 重点准备网络连通性、Kimi API 可用性、讯飞开放平台服务开通状态和对应 API 凭证，而不是本地推理环境。
3. 数据库选型与部署配置

- 首选： MySQL 8.0+
- 最低配置建议：
  - CPU：4 核以上
  - 内存：8GB 以上
  - SSD：100GB 起
- 配置要求：
  - `utf8mb4` 编码
  - 开启定期备份
  - 建立分类字段索引、名称索引、详细地点索引
- 安全要求：
  - 数据库账号分权
  - 生产环境内网访问
  - 日志脱敏
  - 不允许在 DB 中持久化来源身份标识
4. 开发技术栈与环境配置

- 后端： Python 3.11+
- Web/API： FastAPI
- 任务队列： Celery + Redis
- 数据库 ORM： SQLAlchemy 或 SQLModel
- MySQL 驱动： `pymysql` 或 `mysqlclient`
- 数据校验： Pydantic
- 模型编排：自研脚本或轻量服务封装即可
- 部署容器： Docker 、 Docker Compose
- 配置管理： .env + 密钥管理服务
- 日志与监控：MVP 阶段使用应用日志文件和基础错误告警即可
- 开发环境要求：
  - 已下载 `iFly-Skills` 仓库并固定版本
  - Python 环境需安装 `requests`
  - Git 仓库与分支规范
  - Kimi API 与讯飞开放平台 API 密钥管理机制
  - 测试环境数据库
  - 可复现的依赖锁定，如 poetry 或 pip-tools
5. 功能测试样例素材要求

- 图文样例
  - 至少 50 份旅游攻略图文。
  - 覆盖单景点、多景点、跨城、多餐饮、多住宿场景。
  - 需包含图片内文字、正文描述、省略表达、口语表达。
- 带音频视频样例
  - 至少 30 条。
  - 覆盖普通话、方言、环境噪声、背景音乐、多人说话。
  - 时长覆盖 30 秒 - 10 分钟 。
- 标注要求
  - 每份样例需有人工标注的切割结果、分类标签、名称、详细地点、标准一句话描述。
  - 标注中需专门纳入难例：同段混合多主题、无明确地点、模糊住宿推荐、景区内餐饮与景点混杂等。
三、落地建议

- 第一阶段优先做 MVP ：先打通 带音频视频 -> 讯飞极速转写 Skill -> Kimi API 标准化 -> 入库 ，因为该链路最符合需求主干。
- 第二阶段补齐 图文 -> Kimi OCR -> Kimi API 对齐 与规则优化，再统一归一化。
- 第三阶段做评测闭环与规则优化，把“切割边界、分类冲突、地点补全准确率”三类问题持续迭代。
- 若目标是尽快上线，建议采用“云模型 API + 本地 FFmpeg/MySQL + Python 编排”的组合，开发速度最快、风险最低。
- 若目标是私有化交付，需提前锁定讯飞 skill 的商用授权方式、Kimi API 的可用性与替代方案、网络接入策略以及资源方案，否则项目周期会明显拉长。
如果你需要，我可以下一步直接把这份内容整理成一版可放进 PRD/技术方案文档的正式章节结构。
